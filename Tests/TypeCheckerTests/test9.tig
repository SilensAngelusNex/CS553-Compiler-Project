let
	var x: int := 1
	var b: int := "string"
	function one(i: int): int = let
									var a: int := i
									function two(j: int): int = j + x + i
								in
									two (x)
								end
in
	one (x)
end
